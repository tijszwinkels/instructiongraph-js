/**
 * Store interface — JSDoc only, no runtime code.
 *
 * All store implementations (hub, fs, sync) conform to this interface.
 *
 * @typedef {import('../types.js').Store} Store
 * @typedef {import('../types.js').SearchQuery} SearchQuery
 * @typedef {import('../types.js').SearchResult} SearchResult
 * @typedef {import('../types.js').InboundQuery} InboundQuery
 */

export {}
